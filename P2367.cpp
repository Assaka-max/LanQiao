#include<iostream>
#include<algorithm>
using namespace std;
const int N = 5e6 + 9;
int a[N];
int diff[N];
int main(){
  ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
  int n, p;
  cin >> n >> p;
  
  return 0;
}